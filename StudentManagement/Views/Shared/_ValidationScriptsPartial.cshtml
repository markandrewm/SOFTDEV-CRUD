@* jQuery validation libraries are already loaded globally in _Layout.cshtml.
   This partial is kept for convention/compatibility with the standard ASP.NET Core MVC template
   in case individual views want to @await Html.PartialAsync("_ValidationScriptsPartial") explicitly. *@
